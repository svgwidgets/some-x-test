<template>
  <g>
    <circle cx="20" cy="20" r="18" fill="none" stroke="#333" stroke-width="2"/>
    
    <g class="pump-impeller" transform-origin="20 20">
      <path d="M 20,8 L 24,16 L 20,20 L 16,16 Z" :fill="pumpColor" />
      <path d="M 32,20 L 24,24 L 20,20 L 24,16 Z" :fill="pumpColor" />
      <path d="M 20,32 L 16,24 L 20,20 L 24,24 Z" :fill="pumpColor" />
      <path d="M 8,20 L 16,16 L 20,20 L 16,24 Z" :fill="pumpColor" />
    </g>
    
    <circle cx="0" cy="20" r="4" fill="#2196F3" stroke="#333" stroke-width="1.5"/>
    <circle cx="40" cy="20" r="4" fill="#FF9800" stroke="#333" stroke-width="1.5"/>
    
    <text x="20" y="-8" text-anchor="middle" font-size="10" font-weight="bold" fill="#333">
      {{ label }}
    </text>
  </g>
</template>

<script setup lang="ts">
import { computed } from 'vue';
import type { DigitalIO } from '@/lib/io';
import type { VisualDefinition, Position } from '@/lib/visuals';
import { getIOColor } from '@/lib/visuals';

interface Props {
  io: DigitalIO;
  visual: VisualDefinition;
  position: Position;
  label?: string;
}

const props = defineProps<Props>();

const pumpColor = computed(() => getIOColor(props.io));
</script>
